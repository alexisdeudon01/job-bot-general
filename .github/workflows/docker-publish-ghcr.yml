name: deprecated-docker-publish-ghcr

on:
  workflow_dispatch:

jobs:
  deprecated:
    name: Deprecated workflow notice
    runs-on: ubuntu-latest
    steps:
      - name: Explain workflow rename
        run: |
          echo "This workflow has been deprecated."
          echo "Use .github/workflows/integration-e2e.yml for integration and E2E tests."
          echo "No image publishing to GHCR is performed in this repository."